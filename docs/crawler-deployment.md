# Crawler deployment

Retain the existing account-domain robots.txt: search discovery is permitted; named training crawlers are denied. The project copy mirrors the local account-domain file, but only /robots.txt at the domain root is effective. Do not deploy a blanket project ban. No account-domain files were changed. Crawling permission does not grant AI content reuse or model-training rights. robots.txt is voluntary, not access control.
